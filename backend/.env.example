# Backend-only example env
# Copy to `backend/.env` (or export these variables) for local dev.

# Required right now (PromptParserAgent -> Gemini direct)
GEMINI_API_KEY=GEMINIKEY

# Optional override (PromptParserAgent defaults to gemini-1.5-flash-latest)
GEMINI_MODEL=gemini-2.5-flash

OPENROUTER_API_KEY=OPENROUTERKEY
OPENROUTER_MODEL=google/gemini-2.5-flash

# HuggingFace API token (for dataset search and download)
# Get one at: https://huggingface.co/settings/tokens
HF_TOKEN=HUGGINGFACEKEY

# Vultr Cloud Training (DISABLED - No GPU access on this account)
# NOTE: Vultr GPU instances (A40/A100) require account approval
# Current setup uses CPU instances which are slower than local M3 Max
# To enable GPU: Contact Vultr support for GPU instance access
VULTR_API_KEY=VULTUREKEY
USE_VULTR_TRAINING=false